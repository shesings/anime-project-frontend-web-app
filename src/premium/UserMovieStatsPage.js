import React from "react";
/**
 * 
 * @returns Page that returns stats on the users' watched anime like hours, years watched, genre etc..
 * Only for premium users
 */
export function UserMovieStatsPage() {
    return <h1>Some fancy stats on movies watched</h1>
}